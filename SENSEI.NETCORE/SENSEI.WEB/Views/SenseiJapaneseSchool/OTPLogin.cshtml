@{
    Layout = "~/Views/Shared/_AuthLayout.cshtml";
    ViewData["Title"] = "OTP Sign In";
}

@* FontAwesome for Premium Icons *@
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css">
<link href="https://fonts.googleapis.com/css2?family=Noto+Serif+JP:wght@900&family=Inter:wght@400;600;700;900&display=swap" rel="stylesheet">

<style>
    :root {
        --primary: #D83236;
        --primary-dark: #b1282c;
        --accent-sakura: #fff5f5;
        --text-main: #1a1a1a;
        --text-muted: #555555;
        --glass-bg: rgba(255, 255, 255, 0.9);
        --glass-border: rgba(216, 50, 54, 0.12);
    }

    body {
        margin: 0;
        padding: 0;
        min-height: 100vh;
        background: linear-gradient(to bottom, #fffbfb 0%, #fff5f5 100%);
        font-family: 'Inter', -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
        overflow-x: hidden;
        position: relative;
    }

    /* Homepage Background Pattern */
    body::before {
        content: "";
        position: fixed;
        top: 0;
        left: 0;
        width: 100vw;
        height: 100vh;
        z-index: -1;
        pointer-events: none;
        background-image: url('../theme/v1/img/photos/sakura.png'), url('../theme/v1/img/photos/sakura.png');
        background-repeat: no-repeat, no-repeat;
        background-position: -5% -5%, 105% 60%;
        background-size: 500px auto, 600px auto;
        opacity: 0.25;
        mix-blend-mode: multiply;
    }

    /* Falling Sakura Animation Container */
    .sakura-container {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        pointer-events: none;
        z-index: 0;
    }

    .sakura-petal {
        position: absolute;
        background-color: var(--primary);
        border-radius: 150% 0 150% 150%;
        opacity: 0.6;
        z-index: 0;
        box-shadow: 0 2px 5px rgba(0, 0, 0, 0.05);
        pointer-events: none;
    }

    /* Navigation Header */
    .nav-header {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        padding: 0.6rem 0;
        display: flex;
        align-items: center;
        justify-content: center;
        z-index: 1000;
        background: rgba(255, 255, 255, 0.65);
        backdrop-filter: blur(20px);
        -webkit-backdrop-filter: blur(20px);
        border-bottom: 1px solid rgba(255, 225, 217, 0.4);
        box-shadow: 0 4px 30px rgba(0, 0, 0, 0.02);
        transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
    }

    .nav-header::before {
        content: "";
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 3px;
        background: linear-gradient(90deg, var(--primary), #ffb7c5, var(--primary));
        background-size: 200% auto;
        animation: flow 4s linear infinite;
        z-index: 1100;
    }

    @@keyframes flow {
        0% { background-position: 0% 50%; }
        100% { background-position: 200% 50%; }
    }

    .nav-container {
        max-width: 1280px;
        width: 100%;
        margin: 0 auto;
        padding: 0.4rem 2rem;
        display: flex;
        align-items: center;
        justify-content: space-between;
    }

    .nav-brand {
        display: flex;
        align-items: center;
        gap: 0.75rem;
        text-decoration: none;
    }

    .brand-logo-small {
        width: 50px;
        height: 50px;
        object-fit: contain;
    }

    .brand-name-img {
        height: 1.6rem;
        width: auto;
        object-fit: contain;
    }

    .nav-links {
        display: flex;
        gap: 0.5rem;
        align-items: center;
    }

    .nav-links a {
        text-decoration: none;
        color: var(--text-main);
        font-weight: 700;
        padding: 0.6rem 1.2rem;
        border-radius: 99px;
        font-size: 0.95rem;
        transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        position: relative;
    }

    .nav-links a:hover {
        color: var(--primary);
        background: rgba(216, 50, 54, 0.06);
    }

    .login-wrapper {
        width: 100%;
        min-height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 80px 20px 40px;
        position: relative;
        z-index: 10;
        box-sizing: border-box;
    }

    .login-card {
        background: var(--glass-bg);
        backdrop-filter: blur(25px);
        -webkit-backdrop-filter: blur(25px);
        border: 1px solid var(--glass-border);
        border-radius: 32px;
        padding: 50px 45px;
        width: 100%;
        max-width: 460px;
        box-shadow: 0 40px 100px rgba(255, 90, 95, 0.12);
        text-align: center;
    }

    .logo-box {
        margin-bottom: 35px;
    }

    .brand-logo {
        width: 110px;
        height: 110px;
        object-fit: contain;
    }

    .login-title {
        font-weight: 900;
        font-size: 2.3rem;
        color: var(--text-main);
        margin-bottom: 10px;
        letter-spacing: -1px;
    }

    .login-subtitle {
        color: var(--text-muted);
        font-size: 1.1rem;
        margin-bottom: 40px;
    }

    /* Form Styles */
    .form-label {
        display: block;
        text-align: left;
        font-weight: 700;
        color: var(--text-main);
        margin-bottom: 8px;
        font-size: 0.95rem;
    }

    .input-group {
        position: relative;
        margin-bottom: 5px;
    }

    .input-group-text {
        background: rgba(255, 90, 95, 0.05);
        border: 1px solid #ffe0e0;
        border-right: none;
        border-radius: 12px 0 0 12px;
        padding: 0 15px;
        font-weight: 700;
        color: var(--primary);
        display: flex;
        align-items: center;
    }

    .form-control-lg {
        height: 56px !important;
        border-radius: 12px !important;
        border: 1px solid #ffe0e0 !important;
        padding: 10px 20px !important;
        font-weight: 600 !important;
        font-size: 1.1rem !important;
        transition: all 0.2s ease;
        width: 100%;
    }

    .input-group .form-control-lg {
        border-top-left-radius: 0 !important;
        border-bottom-left-radius: 0 !important;
    }

    .form-control-lg:focus {
        border-color: var(--primary) !important;
        box-shadow: 0 0 0 4px rgba(255, 90, 95, 0.08) !important;
        background: #fff !important;
    }

    .btn-primary-premium {
        width: 100%;
        padding: 16px 30px;
        border-radius: 16px;
        font-weight: 700;
        font-size: 1.1rem;
        background: var(--primary);
        color: white;
        border: none;
        cursor: pointer;
        box-shadow: 0 12px 28px rgba(255, 90, 95, 0.3);
        transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 12px;
        margin-top: 25px;
    }

    .btn-primary-premium:hover {
        background: var(--primary-dark);
        transform: translateY(-4px);
        box-shadow: 0 18px 40px rgba(255, 90, 95, 0.4);
    }

    .register-prompt {
        margin-top: 40px;
        padding-top: 30px;
        border-top: 1px solid rgba(255, 90, 95, 0.1);
        font-size: 1.05rem;
        color: var(--text-muted);
    }

    .btn-link-premium {
        color: var(--primary);
        text-decoration: none !important;
        font-weight: 800;
        margin-left: 8px;
    }

    .btn-link-premium:hover {
        text-decoration: underline !important;
    }

    /* Japanese Background Watermark */
    .jp-watermark {
        position: absolute;
        font-family: "Noto Serif JP", serif;
        font-weight: 900;
        color: var(--primary);
        opacity: 0.03;
        font-size: 15rem;
        z-index: -1;
        pointer-events: none;
        white-space: nowrap;
        user-select: none;
    }

    .jp-pos-1 { top: 10%; right: -5%; transform: rotate(15deg); }
    .jp-pos-2 { bottom: 10%; left: -5%; transform: rotate(-15deg); }

    @@media (max-width: 768px) {
        .nav-links { display: none; }
        .nav-header { padding: 1rem 1.5rem; }
        .login-card { padding: 40px 30px; }
    }
</style>

<header class="nav-header">
    <div class="nav-container">
        <a href="~/" class="nav-brand">
            <img src="~/theme/v1/img/photos/logo.PNG" alt="Sensei Logo" class="brand-logo-small" />
            <div class="brand-text">
                <img src="~/theme/v1/img/photos/name1.png" alt="Sensei Japanese Center" class="brand-name-img" />
                <img src="~/theme/v1/img/photos/name.png" alt="Name" class="brand-name-img" />
            </div>
        </a>
        <nav class="nav-links">
            <a href="~/#courses">Courses</a>
            <a href="~/SenseiJapaneseSchool/Gallery">Gallery</a>
            <a href="~/" class="btn-link-home"><i class="fas fa-home"></i> Home</a>
        </nav>
    </div>
</header>

<div class="sakura-container" id="sakura-box"></div>

<div class="jp-watermark jp-pos-1">先生</div>
<div class="jp-watermark jp-pos-2">日本語</div>

<div class="login-wrapper">
    <div class="login-card">
        <div class="logo-box">
            <img src="~/theme/v1/img/photos/logo.PNG" alt="Sensei Logo" class="brand-logo" />
        </div>

        <h1 class="login-title">Welcome back!</h1>
        <p class="login-subtitle">Log in via OTP for secure access</p>

        <form id="otpForm" asp-action="OTPLogin" asp-controller="SenseiJapaneseSchool" method="post">
            <div class="mb-4">
                <label class="form-label">Phone Number</label>
                <div class="input-group">
                    <span class="input-group-text">+94</span>
                    <input type="text" class="form-control form-control-lg" id="phone" name="phone" placeholder="7XXXXXXXX" maxlength="9" />
                </div>
                <span class="text-danger d-block mt-2 text-start small fw-bold" id="phoneError"></span>
            </div>

            <button type="submit" class="btn-primary-premium">
                <i class="fas fa-paper-plane"></i> Send OTP Now
            </button>
        </form>

        <div class="register-prompt">
            Don't have an account? 
            <a asp-controller="SenseiJapaneseSchool" asp-action="Register" class="btn-link-premium">Register Now</a>
        </div>
    </div>
</div>

<script>
    /* Falling Sakura Animation */
    function createPetal() {
        const sakuraBox = document.getElementById('sakura-box');
        if (!sakuraBox) return;

        // Limiting total petals for performance and subtlety
        if (sakuraBox.children.length > 15) return;

        const petal = document.createElement('div');
        petal.className = 'sakura-petal';
        
        const size = Math.random() * 8 + 6;
        petal.style.width = size + 'px';
        petal.style.height = (size * 0.8) + 'px';
        petal.style.left = Math.random() * 100 + 'vw';
        
        const duration = Math.random() * 7 + 5;
        const sway = (Math.random() - 0.5) * 200;
        petal.style.setProperty('--sway', sway + 'px');
        petal.style.animation = `fall ${duration}s linear forwards`;

        sakuraBox.appendChild(petal);

        setTimeout(() => {
            petal.remove();
        }, duration * 1000);
    }

    // Initial burst
    for (let i = 0; i < 8; i++) {
        setTimeout(createPetal, Math.random() * 2000);
    }

    setInterval(createPetal, 800);
</script>

<style>
    @@keyframes fall {
        0% { transform: translate(0, -20px) rotate(0deg); opacity: 0; }
        10% { opacity: 0.8; }
        90% { opacity: 0.8; }
        100% { transform: translate(var(--sway, 100px), 110vh) rotate(360deg); opacity: 0; }
    }
</style>

@section Scripts {
    <script type="text/javascript">
        $(document).ready(function () {
            const $phone = $("#phone");
            const $error = $("#phoneError");

            // Block non-numeric keys
            $phone.on("keypress", function (e) {
                let charCode = e.which ? e.which : e.keyCode;
                if (charCode === 8) return;
                if (charCode < 48 || charCode > 57) {
                    e.preventDefault();
                }
            });

            // Prevent paste of non-numeric values
            $phone.on("paste", function (e) {
                let pasteData = e.originalEvent.clipboardData.getData("text");
                if (!/^\d+$/.test(pasteData)) {
                    e.preventDefault();
                    showError("Only numbers are allowed");
                }
            });

            // Live validation
            $phone.on("input", function () {
                let value = $phone.val();
                value = value.replace(/\D/g, '');
                $phone.val(value);

                if (value.length === 0) {
                    $error.text("");
                    return;
                }

                if (value.startsWith("0")) {
                    showError("Number cannot start with 0");
                }
                else if (value.length > 9) {
                    showError("Maximum 9 digits allowed");
                }
                else if (value.length < 9) {
                    showError("Enter 9-digit mobile number without 0");
                }
                else {
                    clearError();
                }
            });

            // On submit validation
            $("#otpForm").submit(function (e) {
                let phone = $phone.val();

                if (!/^[1-9][0-9]{8}$/.test(phone)) {
                    e.preventDefault();
                    showError("Enter a valid 9-digit mobile number (without leading 0)");
                    return;
                }

                // Append country code before submit
                $phone.val(phone);
            });

            function showError(message) {
                $error.text(message);
            }

            function clearError() {
                $error.text("");
            }
        });
    </script>
}
