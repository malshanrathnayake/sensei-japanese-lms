@using core_web.Helpers

@{
	Layout = null;
}

<nav class="navbar navbar-expand navbar-light navbar-bg">
	<div class="d-flex align-items-center">
		<a class="sidebar-toggle js-sidebar-toggle ms-2">
			<i class="hamburger align-self-center"></i>
		</a>
		<div class="ms-3 d-flex align-items-center gap-3">
			<div class="brand-text d-none d-lg-flex">
				<span>Sensei Japanese Center</span>
				<img src="~/theme/v1/img/photos/name.png" alt="Name" class="brand-name-img" />
			</div>
			<div class="vr d-none d-lg-block" style="height: 1.5rem; opacity: 0.1;"></div>
			<span style="font-weight: 800; color: var(--text-muted); font-size: 0.85rem; letter-spacing: 0.05em; text-transform: uppercase;">Admin Console</span>
		</div>
	</div>

	<div class="navbar-collapse collapse">
		<ul class="navbar-nav navbar-align">

			<!-- notification -->
			@Html.RenderAction("AdminPortalNotification", "Layout", new { area = "" })

			<!-- message -->
			@* @Html.RenderAction("AdminPortalMessages", "Layout", new { area = "" }) *@


			<!-- profile -->
			@Html.RenderAction("AdminPortalProfile", "Layout", new { area = "" })

		</ul>
	</div>
</nav>