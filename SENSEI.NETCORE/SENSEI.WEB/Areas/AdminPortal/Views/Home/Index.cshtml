@model SENSEI.WEB.Models.AdminDashboardViewModel
@{
    ViewData["Title"] = "Admin Dashboard";
}

<style>
    /* Premium Dashboard Styling */
    .stat-card {
        border-radius: 20px;
        border: 1px solid rgba(216, 50, 54, 0.08);
        box-shadow: 0 4px 20px rgba(0, 0, 0, 0.02);
        transition: all 0.3s ease;
        background: #fff;
        overflow: hidden;
    }

    .stat-card:hover {
        transform: translateY(-5px);
        box-shadow: 0 12px 30px rgba(216, 50, 54, 0.1);
    }

    .stat-icon {
        width: 48px;
        height: 48px;
        border-radius: 12px;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 1.25rem;
    }

    .dashboard-action-btn {
        border-radius: 14px;
        padding: 1rem;
        text-align: center;
        transition: all 0.2s;
        border: 1px dashed #dee2e6;
        background: #fdfdfd;
        color: var(--primary);
        font-weight: 700;
        text-decoration: none;
        display: block;
    }

    .dashboard-action-btn:hover {
        background: var(--accent-sakura);
        border-color: var(--primary);
        border-style: solid;
        color: var(--primary);
        transform: scale(1.02);
    }

    .activity-item {
        padding: 12px 0;
        border-bottom: 1px solid #f8f9fa;
        transition: background 0.2s;
    }

    .activity-item:hover {
        background: #fffafa;
    }

    .activity-item:last-child {
        border-bottom: none;
    }

    .hover-primary:hover {
        color: var(--primary) !important;
        text-decoration: underline !important;
    }
</style>

<div class="container-fluid p-0">
    <!-- Page Header & Welcome -->
    <div class="row mb-5 align-items-center">
        <div class="col-8">
            <h1 class="h2 fw-900 text-dark mb-1">Welcome back, <span class="text-primary">@Context.Session.GetString("DisplayName")</span>!</h1>
            <p class="text-muted small fw-600 mb-0">Here's what's happening at Sensei Japanese Center today.</p>
        </div>
        <div class="col-4 text-end">
            <span class="badge bg-soft-primary text-primary px-3 py-2 rounded-pill fw-bold">
                <i class="align-middle me-1" data-feather="calendar"></i> @DateTime.Now.ToString("dd MMM yyyy")
            </span>
        </div>
    </div>

    <!-- Quick Stats Grid -->
    <div class="row mb-4 row-cols-1 row-cols-md-3 row-cols-xl-5 g-3">
        <div class="col">
            <a href="@Url.Action("Index", "Student", new { Area = "AdminPortal" })" class="text-decoration-none">
                <div class="card stat-card border-top border-4 border-primary h-100">
                    <div class="card-body p-3">
                        <div class="row align-items-center">
                            <div class="col mt-0">
                                <h5 class="card-title text-muted fw-bold smaller text-uppercase mb-0" style="font-size: 0.6rem;">Total Students</h5>
                            </div>
                            <div class="col-auto">
                                <div class="stat-icon bg-soft-primary text-primary" style="width: 32px; height: 32px; font-size: 0.8rem;">
                                    <i data-feather="users" style="width: 14px; height: 14px;"></i>
                                </div>
                            </div>
                        </div>
                        <h2 class="mt-2 mb-2 fw-900 text-dark">@Model.TotalStudents</h2>
                        <div class="mb-0">
                            <span class="text-primary smaller fw-800" style="font-size: 0.65rem;">View All &rarr;</span>
                        </div>
                    </div>
                </div>
            </a>
        </div>
        <div class="col">
            <a href="@Url.Action("Index", "StudentRegistration", new { Area = "AdminPortal", status = 1 })" class="text-decoration-none">
                <div class="card stat-card border-top border-4 border-info h-100">
                    <div class="card-body p-3">
                        <div class="row align-items-center">
                            <div class="col mt-0">
                                <h5 class="card-title text-muted fw-bold smaller text-uppercase mb-0" style="font-size: 0.6rem;">Pending Regs</h5>
                            </div>
                            <div class="col-auto">
                                <div class="stat-icon bg-soft-info text-info" style="width: 32px; height: 32px; font-size: 0.8rem;">
                                    <i data-feather="user-plus" style="width: 14px; height: 14px;"></i>
                                </div>
                            </div>
                        </div>
                        <h2 class="mt-2 mb-2 fw-900 text-info">@Model.PendingRegistrations</h2>
                        <div class="mb-0">
                            <span class="text-info smaller fw-800" style="font-size: 0.65rem;">Review &rarr;</span>
                        </div>
                    </div>
                </div>
            </a>
        </div>
        <div class="col">
            <a href="@Url.Action("Index", "StudentPayments", new { Area = "AdminPortal" })" class="text-decoration-none">
                <div class="card stat-card border-top border-4 border-danger h-100">
                    <div class="card-body p-3">
                        <div class="row align-items-center">
                            <div class="col mt-0">
                                <h5 class="card-title text-muted fw-bold smaller text-uppercase mb-0" style="font-size: 0.6rem;">Pending Payments</h5>
                            </div>
                            <div class="col-auto">
                                <div class="stat-icon bg-soft-danger text-danger" style="width: 32px; height: 32px; font-size: 0.8rem;">
                                    <i data-feather="credit-card" style="width: 14px; height: 14px;"></i>
                                </div>
                            </div>
                        </div>
                        <h2 class="mt-2 mb-2 fw-900 text-danger">@Model.PendingPayments</h2>
                        <div class="mb-0">
                            <span class="text-danger smaller fw-800" style="font-size: 0.65rem;">Verify &rarr;</span>
                        </div>
                    </div>
                </div>
            </a>
        </div>
        <div class="col">
            <a href="@Url.Action("Index", "Batch", new { Area = "AdminPortal" })" class="text-decoration-none">
                <div class="card stat-card border-top border-4 border-success h-100">
                    <div class="card-body p-3">
                        <div class="row align-items-center">
                            <div class="col mt-0">
                                <h5 class="card-title text-muted fw-bold smaller text-uppercase mb-0" style="font-size: 0.6rem;">Batches</h5>
                            </div>
                            <div class="col-auto">
                                <div class="stat-icon bg-soft-success text-success" style="width: 32px; height: 32px; font-size: 0.8rem;">
                                    <i data-feather="layers" style="width: 14px; height: 14px;"></i>
                                </div>
                            </div>
                        </div>
                        <h2 class="mt-2 mb-2 fw-900 text-dark">@Model.TotalBatches</h2>
                        <div class="mb-0">
                            <span class="text-success smaller fw-800" style="font-size: 0.65rem;">Manage &rarr;</span>
                        </div>
                    </div>
                </div>
            </a>
        </div>
        <div class="col">
            <a href="@Url.Action("Index", "Course", new { Area = "AdminPortal" })" class="text-decoration-none">
                <div class="card stat-card border-top border-4 border-warning h-100">
                    <div class="card-body p-3">
                        <div class="row align-items-center">
                            <div class="col mt-0">
                                <h5 class="card-title text-muted fw-bold smaller text-uppercase mb-0" style="font-size: 0.6rem;">Courses</h5>
                            </div>
                            <div class="col-auto">
                                <div class="stat-icon bg-soft-warning text-warning" style="width: 32px; height: 32px; font-size: 0.8rem;">
                                    <i data-feather="book" style="width: 14px; height: 14px;"></i>
                                </div>
                            </div>
                        </div>
                        <h2 class="mt-2 mb-2 fw-900 text-dark">@Model.TotalCourses</h2>
                        <div class="mb-0">
                            <span class="text-warning smaller fw-800" style="font-size: 0.65rem;">Programs &rarr;</span>
                        </div>
                    </div>
                </div>
            </a>
        </div>
    </div>

    <div class="row">
        <!-- Main Dashboard Column -->
        <div class="col-12 col-lg-8">
            <div class="card mb-4" style="border-radius: 20px; border: none; box-shadow: 0 4px 15px rgba(0,0,0,0.03);">
                <div class="card-header bg-white border-0 py-4 px-4 d-flex justify-content-between align-items-center">
                    <h5 class="card-title mb-0 fw-800">Recent Registrations</h5>
                    <a href="@Url.Action("Index", "StudentRegistration", new { Area = "AdminPortal" })" class="btn btn-sm btn-outline-primary rounded-pill px-3">View All</a>
                </div>
                <div class="card-body p-0">
                    <div class="table-responsive">
                        <table class="table table-hover mb-0">
                            <thead class="bg-light bg-opacity-50">
                                <tr>
                                    <th class="ps-4">Student Name</th>
                                    <th>Course</th>
                                    <th>Phone</th>
                                    <th class="text-end pe-4">Status</th>
                                </tr>
                            </thead>
                            <tbody>
                                @if(Model.RecentRegistrations != null && Model.RecentRegistrations.Any()) {
                                    foreach(var reg in Model.RecentRegistrations) {
                                        <tr>
                                            <td class="ps-4">
                                                <div class="d-flex align-items-center">
                                                    <div class="avatar-soft bg-soft-primary text-primary rounded-circle me-3 d-flex align-items-center justify-content-center" style="width: 35px; height: 35px;">
                                                        @reg.FirstName[0]@reg.LastName[0]
                                                    </div>
                                                    <div>
                                                        <div class="fw-bold text-dark">
                                                            <a href="@Url.Action("Details", "StudentRegistration", new { Area = "AdminPortal", q = reg.EncryptedKey })" class="text-dark text-decoration-none hover-primary">@reg.StudentRegistrationPopulatedName</a>
                                                        </div>
                                                        <div class="text-muted smaller" style="font-size: 0.65rem;">@reg.CreatedDateTime.ToString("MMM dd, yyyy")</div>
                                                    </div>
                                                </div>
                                            </td>
                                            <td class="vertical-align-middle">@reg.Course?.CourseName</td>
                                            <td class="vertical-align-middle fw-600">@reg.PhoneNo</td>
                                            <td class="text-end pe-4 vertical-align-middle">
                                                @if (reg.IsApproved)
                                                {
                                                    <span class="badge bg-soft-success text-success rounded-pill px-3">Approved</span>
                                                }
                                                else if (reg.IsRejected)
                                                {
                                                    <span class="badge bg-soft-danger text-danger rounded-pill px-3">Rejected</span>
                                                }
                                                else
                                                {
                                                    <span class="badge bg-soft-warning text-warning rounded-pill px-3">Pending</span>
                                                }
                                            </td>
                                        </tr>
                                    }
                                } else {
                                    <tr>
                                        <td colspan="4" class="text-center py-4 text-muted">No recent registrations found.</td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>

        <!-- Sidebar Dashboard Column -->
        <div class="col-12 col-lg-4">
            <!-- Quick Actions -->
            <div class="card mb-4" style="border-radius: 20px; border: none; box-shadow: 0 4px 15px rgba(0,0,0,0.03);">
                <div class="card-header bg-white border-0 py-4 px-4">
                    <h5 class="card-title mb-0 fw-800 text-dark">Quick Actions</h5>
                </div>
                <div class="card-body pt-0 px-4 pb-4">
                    <div class="row g-3">
                        <div class="col-6">
                            <a href="@Url.Action("Index", "Batch", new { Area = "AdminPortal" })" class="dashboard-action-btn">
                                <i class="d-block mb-1" data-feather="plus-circle"></i>
                                New Batch
                            </a>
                        </div>
                        <div class="col-6">
                            <a href="@Url.Action("Index", "StudentRegistration", new { Area = "AdminPortal", status = 1 })" class="dashboard-action-btn">
                                <i class="d-block mb-1" data-feather="user-check"></i>
                                Approvals
                            </a>
                        </div>
                        <div class="col-6">
                            <a href="@Url.Action("Index", "Course", new { Area = "AdminPortal" })" class="dashboard-action-btn">
                                <i class="d-block mb-1" data-feather="book-open"></i>
                                Our Courses
                            </a>
                        </div>
                        <div class="col-6">
                            <a href="@Url.Action("Index", "StudentPayments", new { Area = "AdminPortal" })" class="dashboard-action-btn">
                                <i class="d-block mb-1" data-feather="credit-card"></i>
                                Payments
                            </a>
                        </div>
                    </div>
                </div>
            </div>

        </div>
    </div>
</div>

@section Scripts {
    <script>
        $(document).ready(function() {
            if (window.feather) { feather.replace(); }
        });
    </script>
}